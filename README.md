# Tson Configurations syntax support

Simple plugin to support
[Tson Configuraions](https://github.com/Te4hnoPlus/TsonConfigurations) syntax for `VS Code`

## Building

1) install `npm install -g @vscode/vsce`
2) run `build.bat`